const StartFunc = () => {
    var $table = $('#table2DaysBefore');

    const finalOptions = {
        // onClickRow: StartFuncOnClickRowFunc
    };

    $table.bootstrapTable(finalOptions);
};

export { StartFunc };
